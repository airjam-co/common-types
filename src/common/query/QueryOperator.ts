export enum QueryOperator {
    AND = "and",
    OR = "or",
    NOT = "not",
    LESS_THAN = "lt",
    LESS_THAN_EQ = "lte",
    GREATER_THAN = "gt",
    GREATER_THAN_EQ = "gte",
    IN = "in",
    NOT_IN = "nin",
}
